name: storybook
description: 儿童绘本朗读App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  sqflite: ^2.4.1
  path_provider: ^2.1.5
  image_picker: ^1.1.2
  flutter_tts: ^4.2.1
  file_picker: ^8.1.6
  native_pdf_renderer: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
